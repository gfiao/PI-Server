<style>
    .a-bit-of-margin {
        margin-left: 20px;
    }

    .a-bit-of-margin-button {
        margin-top: 15px;
        margin-bottom: 30px;
    }

    .btn-group {
        display: inline-block;
    }
</style>

<div id="cont1" class="container space-top space-bottom">
  <h2 class="page-header">Editar perfil</h2>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: {method: :put}) do |f| %>
      <%= devise_error_messages! %>

      <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
          <div>Currently waiting confirmation for: <%= resource.unconfirmed_email %></div>
      <% end %>

      <div class="row">
        <div class="form-group col-md-8">
          <%= f.label :sobre_mim %></br>
          <%= f.text_area :about_me, class: 'form-control' %>
        </div>
      </div>

      <div class="row">
        <div class="form-group col-md-8">
          <%= f.label :nova_imagem_de_perfil %></br>
          <%= f.text_field :avatar_url, class: 'form-control' %>
        </div>
      </div>

      <div class="row">
        <div class="form-group col-md-6">
          <%= f.label :nova_password %> <i>(deixe em branco se não quiser mudar)</i><br/>
          <%= f.password_field :password, autocomplete: "off", class: 'form-control' %>
        </div>
      </div>

      <div class="row">
        <div class="form-group col-md-6">
          <%= f.label :confirma_a_nova_password %>
          <br/>
          <%= f.password_field :password_confirmation, autocomplete: "off", class: 'form-control' %>
        </div>
      </div>

      <div class="row">
        <div class="form-group col-md-6">
          <%= f.label :password_atual %> <i>(precisamos da palavra-passe atual para confirmar as mudanças)</i><br/>
          <%= f.password_field :current_password, autocomplete: "off", class: 'form-control' %>
        </div>
      </div>

      <div class="row" style="margin-top: 1%">
        <span class="col-md-6 btn-group">
          <%= f.submit "Atualizar informação", class: "btn btn-default" %>
          <%= button_to "Voltar atrás", :back, method: :get, class: "btn btn-default a-bit-of-margin" %>
        </span>
      </div>

  <% end %>

  <h3 class="page-header">Cancelar conta</h3>

  <p>Caso queiras apagar a tua conta de vez, basta carregares no botão abaixo.<br>
    Tem em atenção que esta acção é <strong>permanente</strong>, depois não será possível obter informação da tua
    conta!
  </p>

  <p><%= button_to "Cancelar a minha conta",
                   registration_path(resource_name), data: {confirm: "Tem a certeza?"}, method: :delete, class: "btn btn-default a-bit-of-margin-button" %></p>
</div>
