<%= form_for(@current_video) do |f| %>
  <% if @current_video.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@current_video.errors.count, "error") %> prohibited this current_video from being saved:</h2>

      <ul>
      <% @current_video.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :index %><br>
    <%= f.number_field :index %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
