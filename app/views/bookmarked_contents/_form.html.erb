<%= form_for(@bookmarked_content) do |f| %>
  <% if @bookmarked_content.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@bookmarked_content.errors.count, "error") %> prohibited this bookmarked_content from being saved:</h2>

      <ul>
      <% @bookmarked_content.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :content_id %><br>
    <%= f.number_field :content_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
